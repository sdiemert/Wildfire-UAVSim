[pytest]
testpaths = tests
# the project modules (policy, config, agents, ...) live in the repository root, not in an installed
# package, so the root has to be importable from the tests
pythonpath = .
addopts = -v
