"""Development tooling that is not part of the simulator.

Nothing under sim/ imports from here. These are the scripts that check and report on the repository
itself, and they are importable as a package so that the test suite can call into them.
"""
